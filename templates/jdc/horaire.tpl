<h2>Mon horaire journalier</h2>

{if $src != ''}
    <img src="{$src}" alt="{$matricule}" class="img-responsive center-block">
    {else}
    <p class="avertissement">Horaire non disponible pour l'instant</p>
{/if}
